META_APP_ID=1343077727979873

// INTENDED, KEY IS PUBLIC, NOT SECRET. DO NOT USE THIS KEY FOR ANYTHING OTHER THAN TESTING.

# AI providers (see docs/ai.md). Both default to the vendor-free placeholder,
# so the generation pipeline runs end-to-end with no keys. Swapping to a real
# vendor is a config change here (plus registering the provider in code).
AI_TEXT_PROVIDER=placeholder
AI_IMAGE_PROVIDER=placeholder